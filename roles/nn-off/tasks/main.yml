---
# tasks file for nn-off
 - debug:
     var: "{{ item }}"
   with_dict: "{{ u.disabled }}"


 - name: disable units
   systemd:
     name: "{{ item }}"
     state: stopped
     enabled: no
     masked: yes
   with_dict: "{{ u.disabled }}"