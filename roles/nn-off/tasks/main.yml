---
# tasks file for nn-off
 - debug:
     var: "{{ item }}"
   with_list: "{{ un.disb }}"


 - name: disable units
   become: yes
   systemd:
     name: "{{ item }}"
     state: stopped
     enabled: no
     masked: yes
   with_list: "{{ un.disb }}"