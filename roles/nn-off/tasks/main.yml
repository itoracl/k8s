---
# tasks file for nn-off
 - debug:
     var: "{{ item.key }}"
   with_dict: "{{ un.disb }}"


 - name: disable units
   systemd:
     name: "{{ item.key }}"
     state: stopped
     enabled: no
     masked: yes
   with_dict: "{{ un.disb }}"