---
# handlers file for containerd

 - name: reload systemd
   become: yes
   systemd:
     daemon-reload: true
     
 - name: restart containerd
   become: yes
   systemd:
     name: containerd
     enabled: true
     state: restarted
